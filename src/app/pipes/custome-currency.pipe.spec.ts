import { CustomeCurrencyPipe } from './custome-currency.pipe';

describe('CustomeCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomeCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
