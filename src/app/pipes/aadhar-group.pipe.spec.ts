import { AadharGroupPipe } from './aadhar-group.pipe';

describe('AadharGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new AadharGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
