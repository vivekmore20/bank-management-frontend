import { DepositWithdrawColorPipe } from './deposit-withdraw-color.pipe';

describe('DepositWithdrawColorPipe', () => {
  it('create an instance', () => {
    const pipe = new DepositWithdrawColorPipe();
    expect(pipe).toBeTruthy();
  });
});
